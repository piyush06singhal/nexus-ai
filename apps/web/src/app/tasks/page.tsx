import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Tasks — NEXUS" };

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      description="Tasks are discrete, trackable units of work assigned to agents. A future task engine will queue, execute, and verify them with full execution IDs and observability. This area is a Phase 0 placeholder."
    />
  );
}