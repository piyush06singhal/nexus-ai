import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Approvals — NEXUS" };

export default function ApprovalsPage() {
  return (
    <PlaceholderPage
      title="Approvals"
      description="Approvals are sensitive actions that pause until a human grants permission — the security boundary between autonomous execution and external side effects. This area is a Phase 0 placeholder."
    />
  );
}