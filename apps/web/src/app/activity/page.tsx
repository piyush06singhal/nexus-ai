import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Activity — NEXUS" };

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity"
      description="Activity is a live feed of agent actions, tool calls, and system events — the observability surface for everything the workforce does. This area is a Phase 0 placeholder."
    />
  );
}