import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Agents — NEXUS" };

export default function AgentsPage() {
  return (
    <PlaceholderPage
      title="Agents"
      description="Agents are individual AI workers with roles, capabilities, tools, and memory. The agent runtime (Phase 1+ of the roadmap) will power their reasoning, tool use, and collaboration. This area is a Phase 0 placeholder."
    />
  );
}