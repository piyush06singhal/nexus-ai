import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Settings — NEXUS" };

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Settings will cover workforce configuration, AI provider credentials, security, permissions, and infrastructure controls. Secrets are never hardcoded — they come from environment variables. This area is a Phase 0 placeholder."
    />
  );
}