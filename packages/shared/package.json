{
  "name": "@nexus/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for the NEXUS monorepo (API ↔ Web contract)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
